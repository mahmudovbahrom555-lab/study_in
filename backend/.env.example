# Server
SERVER_PORT=8080
SERVER_ENV=development
SERVER_VERSION=0.1.0
# CORS: comma-separated origins. Default * (dev only). In production set explicitly:
# SERVER_ALLOWED_ORIGINS=https://repetapp.uz,https://app.repetapp.uz
SERVER_ALLOWED_ORIGINS=*

# Database
DATABASE_URL=postgres://repetapp:repetapp_dev_password@localhost:5432/repetapp?sslmode=disable
DATABASE_MAX_CONNS=25

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT
JWT_ACCESS_SECRET=change_me_in_production_to_random_string_min_32_chars
JWT_REFRESH_SECRET=change_me_in_production_to_another_random_string_min_32
JWT_ACCESS_TTL_MINUTES=15
JWT_REFRESH_TTL_DAYS=30

# S3 / MinIO
S3_ENDPOINT=localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin_dev
S3_BUCKET=repetapp-files
S3_USE_SSL=false
S3_REGION=us-east-1

# SMS (Eskiz.uz)
SMS_PROVIDER=mock
SMS_ESKIZ_EMAIL=
SMS_ESKIZ_PASSWORD=
SMS_ESKIZ_FROM=4546

# Firebase Cloud Messaging
FCM_CREDENTIALS_PATH=
FCM_PROJECT_ID=

# Sentry
SENTRY_DSN=

# Logging
LOG_LEVEL=debug
LOG_FORMAT=text

# Auto-migrate: set true to run migrations on startup (useful in Docker)
AUTO_MIGRATE=false

# OpenAI / AI features
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# Per-user monthly token cap (0 = unlimited). 500k ≈ $0.15 at gpt-4o-mini prices.
AI_MONTHLY_TOKENS_MAX=500000
# Max PDF upload size in bytes (default 10 MB)
AI_MAX_DOC_SIZE_BYTES=10485760
# Chunk size in words for RAG splitting
AI_CHUNK_SIZE=400
AI_CHUNK_OVERLAP=50
